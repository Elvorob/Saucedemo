# pytest.ini
[pytest]
minversion = 6.0
addopts = -v --strict-config --setup-show --html=report/report.html
log_cli=true
log_level=INFO
log_format = %(asctime)s %(levelname)s %(message)s
log_date_format = %Y-%m-%d %H:%M:%S