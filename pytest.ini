# pytest.ini
[pytest]
minversion = 6.0
addopts = -v --strict-config
log_cli=true
log_level=INFO
log_format = %(asctime)s %(levelname)s %(message)s
log_date_format = %Y-%m-%d %H:%M:%S
markers =
    open_page:
    view_products:
;    elina_abramova:name of tester